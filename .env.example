# Copy to .env and fill in. .env is git-ignored and must NEVER be committed.
# Used only by the local import script (scripts/import-samples.ts).
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-5
